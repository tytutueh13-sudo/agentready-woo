REVENUE_SYSTEM_ENABLED=true
PRODUCT_early-3426536d88daa242_ENABLED=true
# Never commit real secrets here — Cloudflare secret bindings only (section 33).
X402_NETWORK=eip155:84532
X402_ASSET=
X402_PAY_TO=
X402_FACILITATOR_URL=
REAL_PAYMENTS_ENABLED=false
D1_REAL_CONCURRENCY_VERIFIED=false
X402_WIRE_INTEROP_VERIFIED=false

# --- AgentReady Woo merchant configuration (set as Worker secret bindings) ---
# The merchant's own WooCommerce store REST API (their keys, no upstream cost).
WOO_STORE_URL=https://your-store.example.com
WOO_CONSUMER_KEY=ck_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
WOO_CONSUMER_SECRET=cs_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# HMAC secret for signed, expiring add-to-cart links (openssl rand -hex 32).
CART_SIGNING_SECRET=
# Public base URL agents should use (e.g. https://mcp.utilityhouse.xyz).
PUBLIC_BASE_URL=
